pipeline { 
    agent any 
    stages {
        
        
        stage('Terraform-init-plan'){
            steps {
                
                sh "$WORKSPACE/deploy.sh ${COMMAND}"
            }
        }
        
        stage('Cleanup'){
            steps {
                
                cleanWs()
            }
        }
       
    }
}
