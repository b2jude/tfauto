pipeline {
    agent any
    stages {


        stage('Terraform-init-plan'){
            steps {
                cd $WORKSPACE/${SUBSYSTEM}
                sh "./deploy.sh ${COMMAND}"

            }
        }

        stage('Cleanup'){
            steps {

                cleanWs()
            }
        }

    }
}
