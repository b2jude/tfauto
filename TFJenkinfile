pipeline {
    agent any
    stages {


        stage('Terraform-init-plan'){
            steps {
                cd $WORKSPACE/${SUBSYSTEM}
                sh "./deploy.sh ${COMMAND}"
              #sh "$WORKSPACE/deploy.sh ${COMMAND}"
            }
        }

        stage('Cleanup'){
            steps {

                cleanWs()
            }
        }

    }
}
