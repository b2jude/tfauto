pipeline { 
    agent any 
    stages {
        stage('WS Clean') { 
            steps { 
                cleanWs() 
            }
        }
        
        stage('Terraform-init-plan'){
            steps {
                sh "terraform --version"
            }
        }
        
        stage('WS clean-end') {
            steps {
                cleanWs()
            }
        }
    }
}
