pipeline { 
    agent any 
    stages {
        stage('WS Clean') { 
            steps { 
                cleanWs() 
            }
        }
        stage('Terraform init'){
            steps {
                terraform init
                terraform plan
            }
        }
        stage('WS clean-end') {
            steps {
                cleanWs()
            }
        }
    }
}
