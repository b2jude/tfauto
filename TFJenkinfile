pipeline { 
    agent any 
    stages {
        stage('WS Clean') { 
            steps { 
                cleanWs() 
            }
        }
        
        stage('Terraform-init-plan'){
            steps {
                #sh "terraform --version"
                #sh "/usr/local/bin/terraform init /var/lib/jenkins/workspace/test-pipeline"
                sh "./deploy.sh"
            }
        }
        
        stage('WS clean-end') {
            steps {
                cleanWs()
            }
        }
    }
}
